export default class ItemDetail {
    constructor(id, itemId, photo, detailDescription) {
        this.id = id;
        this.itemId = itemId;
        this.photo = photo;
        this.detailDescription = detailDescription;
    }
}